Removed from claxton connections db query

create table tax_rate
(tax_id char(3) not null PRIMARY KEY,
tax_label varchar(25) not null,
rates decimal not null);

removed from customers table
username varchar(20) FOREIGN KEY references login(username)

from left nav bar in layout

<ul>Guestbook
            <li>View Guestbook</li>
            <li>Sign Guestbook</li>
            </ul>

